# Full workspace dependencies (Magician + MG400 + visualizer).
#
# Install:
#   pip install -U pip
#   pip install -r requirements.txt
#
# Notes:
# - `dobot_api.py` for MG400 is provided by vendor/TCP-IP-4Axis-Python (SDK clone),
#   while `numpy` is its runtime dependency.
# - `pydobot` is kept for Track C legacy/reference compatibility.

# Magician control stacks
pydobotplus==0.1.2
pydobot==1.3.2
pyserial==3.4

# GUI and plotting (RobotViz + MG400 joint GUI)
PyQt5>=5.15.11,<5.16
pyqtgraph>=0.14.0,<0.15

# Numeric support (feedback packet dtype parsing, viz helpers)
numpy>=1.26,<3
