# mg400/requirements.txt — minimal dependencies for MG400 intro scripts
#
# Install with:
#   pip install -r requirements.txt
#
# numpy — used internally by dobot_api.py (MyType numpy dtype for feedback packets)
#
# Note: dobot_api.py must be available via the SDK git clone:
#   cd dobot_ws
#   git clone https://github.com/Dobot-Arm/TCP-IP-4Axis-Python.git vendor/TCP-IP-4Axis-Python
# See ../../docs/README.md and ../../../mg400/MG400_info.md for setup notes.

numpy>=1.26,<3
